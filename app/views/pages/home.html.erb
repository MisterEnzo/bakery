<div class="container banner">
  <h1>Momma's Gluten Free Bakes</h1>
  <p>Your home for gluten-free goodies</p>
</div>

<div class="container">
  <div class="row">
    <% @menu_items.each do |item| %>
      <div class="col-md-4">
        <%= image_tag item.photo_url, height: 200 %>
        <p><%= item.name %></p>
        <p><%= item.description %></p>
        <p><%= humanized_money_with_symbol(item.price) %></p>

      </div>
    <% end %>
    <%= form_tag orders_path do %>
    <%#= hidden_field_tag 'menu_item_id', item.id %>
    <%= submit_tag 'Place an Order', class: 'btn btn-primary' %>
    <% end %>
  </div>
</div>
