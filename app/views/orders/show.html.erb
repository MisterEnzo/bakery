<h1>Order</h1>
<ul>
  <li>Status: <%= @order.state %></li>
  <li>Order ID: <%= @order.id %></li>
  <li>Order Contents:
    <% @order.order_details.each do |detail| %>
      <p><%= detail.quantity %> <%= detail.menu_item.name %></p>
    <% end %>
  </li>
  <li>Amount: <%= humanized_money_with_symbol(@order.amount) %></li>
<ul>
